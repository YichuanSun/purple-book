#include <bits/stdc++.h>
#define INF 10000005
using namespace std;
string ss;
int main()  {
    int n,a=INF,b,c,d;
    cin>>n;
    for (int i=0;i<n;i++)   {
        cin>>ta>>tb>>tc>>td;
        a=min(ta,a),b=min(tb,b),c=min(tc,c),d=min(td,d);

    }

    return 0;
}
